<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.Pilotproject.dao.MemberDAO">

	<select id="selectAllMember" resultType="com.springboot.Pilotproject.dto.MemberDTO">
		select * from member order by mno desc
	</select>

	<select id="selectMemberById" parameterType="String"
	        resultType="com.springboot.Pilotproject.dto.MemberDTO">
		select * from member where mid = #{mid}
	</select>

	<insert id="insertMember">
		insert into member(
		    mno, mid, mpasswd, mname, mtel, mbirth, mprofile, mzip,
		    memail, mmemo, mgender, mhobby, mdate, mauthority
		) values(
		    member_seq.nextval, #{mid}, #{mpasswd}, #{mname}, #{mtel},
		    #{mbirth}, #{mprofile}, #{mzip},
		    #{memail}, #{mmemo}, #{mgender}, #{mhobby}, sysdate, #{mauthority}
		)
	</insert>

	<update id="updateMember">
		update member
		set mid       = #{mid},
		    mpasswd   = #{mpasswd},
		    mname     = #{mname},
		    mtel      = #{mtel},
		    mbirth    = #{mbirth},
		    mprofile  = #{mprofile},
		    mzip      = #{mzip},
		    memail    = #{memail},
		    mmemo     = #{mmemo},
		    mgender   = #{mgender},
		    mhobby    = #{mhobby},
		    mauthority = #{mauthority}
		where mno = #{mno}
	</update>

	<delete id="deleteMember">
		delete from member
		where mno = #{mno}
	</delete>

</mapper>